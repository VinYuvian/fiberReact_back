apiVersion: v1
kind: ConfigMap
metadata:
  name: fiber-conf
  namespace: jenkins
data:
  .env: |
    DB_CONN_STRING=$DB_CONN_STRING
    DATABASE=$db_info_psw
    USERNAME=$db_user_usr
    PASSWORD=$db_user_psw
